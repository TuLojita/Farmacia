

export const registerItem = (objet) => {

    let res = {
        error: false,
        message: ''
    }

    // errores


    // const newRegister = localStorage.setItem('items', JSON.stringify(objet));

    return res;
}